apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Cluster prerequisites. Applying this does not deploy the application.
resources:
  - namespaces.yaml
  - service-account.yaml
  - vault-auth.yaml
